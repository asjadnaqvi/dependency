# The GitHub Pages app must serve raw data files as plain text/JSON.
# Do not apply Git LFS to the deployment bundle; otherwise the browser receives
# pointer files instead of valid JSON payloads.
dependency/data/** -filter -diff -merge -text
