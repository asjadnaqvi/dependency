# The GitHub Pages app needs Git LFS to materialize the repository's data files
# during checkout; otherwise the deployment bundle contains pointer files
# instead of valid JSON payloads.
data/** filter=lfs diff=lfs merge=lfs -text
